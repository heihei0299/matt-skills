node_modules/
pi
.codegraph/
.cortexkit/
pi-session-*.html
.claude/
.mcp.json

# 敏感文件与日志（不入库）
.env
.env.*
!.env.example
*.log

# 嵌套独立仓库（pi-session-anylize），非本仓库内容
spec/

# 本地开发符号链接（指向 ~/.cc-switch/skills/），不入库
.agents/skills/skill-creator

# 备份文件（init --force / sync 自动备份）
*.bak
.opencode.bak/
.pi.bak/
.scratch/
