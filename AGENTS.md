# 安全铁律（IMPORTANT）

* 不提交密钥、token、`.env`；push 前检查硬编码凭据。
* 未经确认不执行 `rm -rf`、force push、`reset --hard`、`checkout .` 或其他带 `--force` 的破坏性操作。
* 不读取 `.env`、`*.key`、`*secret*`，除非用户显式要求。
* 同一任务禁止重复启动 `code-view`；已有等价代码上下文时不得为确认、复核或提高信心再次启动。

## 编译授权铁律

任何可能直接或间接触发编译的命令，均须针对当前任务和环境获得明确授权；修复、测试、发布或运行 CI 不自动授权本机编译。

获得测试授权时，仅使用 `GOMAXPROCS=2`、`-p 1`、`-parallel 1` 串行执行 root 与 standalone 全量测试。

本机 release 构建统一交由 GitHub Actions，其他本机编译、安装、打包、交叉编译及发布操作均须单独授权。

# 执行原则

* 禁用 grep/find；内容搜索用受限 rg，文件搜索用受限 fd，定位后立即 read 最小必要范围，禁止无边界搜索。
* 信息足够时直接行动；仅当关键歧义会实质改变结果时澄清。
* 从最小必要上下文开始，按需深入。
* 局部最小修改，不扩大任务范围，不添加无依据的兜底逻辑。
* 脏工作区不回滚、覆盖或混入已有无关改动。
* 不重复等价的探索、读取、审查、验证或 CI。
* 用户当前指令优先于历史经验和默认流程。
* 执行类任务给短进度；分析类任务优先给结论、依据和权衡。

## 验证与测试

* 默认只验证本次修改及直接受影响路径；优先相关单测、单文件测试、模块测试和原复现路径。
* 修复什么就测试什么；根据验证反馈继续修正，不重复等价检查。
* 不因每个小步骤自动扩大测试范围，也不把每个 Red/Green cycle 都升级为全量测试。
* 仅当修改跨模块、触及公共接口或核心基础设施、局部验证不足以证明正确、进入发布/合并最终验收，或用户明确要求时，才考虑扩大验证范围。
* 是否执行任何测试、lint、typecheck、build 或其他可能触发编译的命令，仍服从“编译授权铁律”。

## Review

* 默认只 review 本轮 diff、修改文件及直接受影响调用链。
* 修复后只复验新增修改和此前未通过项，不重复审查无关代码。
* 不因进入下一轮自动执行全量 review。
* 仅当影响面明显扩大、局部 review 无法建立足够证据、进入发布/合并最终验收，或用户明确要求时扩大 review 范围。
* 实现与验收优先由两个独立角色完成：一个模型执行，一个模型 review；Reviewer 发现问题后返回 Executor 修复，再对新增修改与未解决项复验，循环直到无 actionable findings。

# Harness 原则

* 以当前环境实际提供的工具、代码和可验证输出为准，不假定不存在的能力。
* 能由代码、测试、lint、类型或工具机械约束的规则，不持续堆进提示词。
* 同类失败反复出现时，优先改进工具、反馈、结构或可发现知识，而不是重复尝试。
* 只在需要用户判断或授权时升级给用户。

# 包管理

## Node.js

* 使用 `pnpm`。
* 安装：`pnpm install`
* 一次性执行：`pnpm dlx <pkg>`
* 锁文件：`pnpm-lock.yaml`

## Python

* 使用 `uv`，禁止 `pip`、`poetry`、`pipenv`。
* 安装：`uv sync`
* 执行：`uv run`
* 一次性执行：`uvx`
* 锁文件：`uv.lock`

## 通用

* 禁止全局安装。
* 所有安装、测试、lint、typecheck 等仍受“编译授权铁律”约束。

# 长任务

* 多步任务除非遇到需要用户决策或授权的点，否则不中途结束回合。
* 预告下一步后立即继续，不等待用户回复“继续”。
