name: Publish to npm (deprecated — migrated to ci.yml)

# 已迁移至 .github/workflows/ci.yml 的 publish job（单文件锁死）
# 本文件仅保留防双发：不再监听 push.tags，改为手动触发占位
on:
  workflow_dispatch:
    inputs:
      tag:
        description: '手动触发占位（实际发布请 push tag 触发 ci.yml）'
        required: false
        type: string

jobs:
  noop:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "publish.yml 已废弃，发布由 ci.yml 的 publish job 承接（verify→publish 单文件不可绕过）"
